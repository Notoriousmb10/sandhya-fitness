{
  "name": "sandhya-fitness",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "concurrently \"npm --prefix frontend run dev\" \"npm --prefix backend run start\"",
    "frontend": "npm --prefix frontend run dev",
    "backend": "nodemon --prefix backend run start",
    "dev": "concurrently \"npm --prefix frontend run dev\" \"npm --prefix backend run dev\"",
    "build": "npm --prefix frontend run build",
    "lint": "eslint .",
    "preview": "npm --prefix frontend run preview"
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "nodemon": "^3.1.4"
  },
  "dependencies": {
    "@react-oauth/google": "^0.12.1",
    "jwt-decode": "^4.0.0",
    "ngrok": "^5.0.0-beta.2"
  }
}
